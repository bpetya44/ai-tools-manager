[PHP]
; Laravel optimized PHP configuration

; Memory settings
memory_limit = 256M
max_execution_time = 60
max_input_time = 60

; File upload settings
upload_max_filesize = 64M
post_max_size = 64M
max_file_uploads = 20

; Session settings
session.gc_maxlifetime = 1440
session.cookie_lifetime = 0

; OPcache settings for better performance
opcache.enable = 1
opcache.enable_cli = 1
opcache.memory_consumption = 128
opcache.interned_strings_buffer = 8
opcache.max_accelerated_files = 4000
opcache.revalidate_freq = 2
opcache.fast_shutdown = 1
opcache.validate_timestamps = 1

; Realpath cache settings
realpath_cache_size = 4096k
realpath_cache_ttl = 600

; Error reporting (development) - hide deprecation warnings
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT & ~E_USER_DEPRECATED
display_errors = On
display_startup_errors = Off
log_errors = On
error_log = /var/log/php_errors.log

; Timezone
date.timezone = Europe/Sofia

; Security settings
expose_php = Off
allow_url_fopen = On
allow_url_include = Off

; Performance settings
output_buffering = 4096
zlib.output_compression = Off

; Laravel specific settings
auto_prepend_file =
auto_append_file =

; Enable assertions for development
assert.active = 1
assert.warning = 1
assert.bail = 0

; Mbstring settings
mbstring.language = neutral
; Removed deprecated mbstring.internal_encoding, mbstring.http_input, mbstring.http_output
mbstring.encoding_translation = Off
